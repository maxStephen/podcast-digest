# Copy to .env for local development — never commit .env

# AWS
AWS_PROFILE=your-named-profile
AWS_REGION=us-east-1
S3_BUCKET=your-podcast-digest-bucket
CONFIG_KEY=config/podcasts.json
LOG_KEY=logs/run_log.jsonl

# Populated by Secrets Manager in Lambda — fill in locally for dev
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
MS_GRAPH_TOKEN=
POCKET_CASTS_EMAIL=
POCKET_CASTS_PASSWORD=
SES_FROM_ADDRESS=
SES_TO_ADDRESS=
